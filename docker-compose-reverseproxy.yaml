services:
  nginx:
    image: nginx:latest
    container_name: nginx-reverseproxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./ssl/eduardovedes:/etc/nginx/ssl/eduardovedes
      - ./ssl/vedespt:/etc/nginx/ssl/vedespt
      - ./ssl/wecraftcode:/etc/nginx/ssl/wecraftcode
      - ./ssl/creatorsguild:/etc/nginx/ssl/creatorsguild
      - ./ssl/remix.creatorsguild:/etc/nginx/ssl/remix.creatorsguild
      - ./ssl/leanly:/etc/nginx/ssl/leanly
networks:
  vedesvps:
    external: true
