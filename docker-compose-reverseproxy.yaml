services:
  nginx:
    image: nginx:latest
    container_name: nginx-reverseproxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./ssl/creatorsguild:/etc/nginx/ssl/creatorsguild
      - ./ssl/eduardovedes:/etc/nginx/ssl/eduardovedes
networks:
  vedesvps:
    external: true
